services:
  subapp:
    image: nopush/podman-compose-test
    command: ["/bin/busybox", "sh", "-c", "env | grep YY"]
    tmpfs:
      - /run
      - /tmp
    env_file:
      - ../env-files/subdir.env


