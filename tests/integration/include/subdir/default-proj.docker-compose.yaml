services:
  defaultapp:
    image: nopush/podman-compose-test
    command: ["/bin/busybox", "sh", "-c", "env | grep XX"]
    tmpfs:
      - /run
      - /tmp
    env_file:
      - ../env-files/default.env

